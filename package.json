{
  "name": "assignment-1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types index.ts",
    "bonus": "node --experimental-strip-types bonus.ts",
    "test": "vitest run",
    "console": "open console.html || xdg-open console.html"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
